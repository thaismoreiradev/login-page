
export const Button = ({title}) => (
    <section>

        <button className='p-3 rounded-xl text-neutral-800 font-medium h-9 flex items-center justify-center
           bg-gradient-to-r from-purple-500 via-orange-200 to-pink-400 text-sm outline-none w-full'
            type="submit"
        >{title}</button>

        
    </section>

)